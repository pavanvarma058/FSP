namespace _1_Guess_A_Word_Game
{
    internal class Program
    {
        static void Main(string[] args)
        {
            // The 'Guess a Word' Game

            string hiddenWord = "elephant";
            int hiddenWordLength = hiddenWord.Length;

            bool[] checkWord = new bool[hiddenWordLength];

            string unguessedLetter;
            for(int i=0; i<checkWord.Length; i++)
            {
                if (checkWord[i])
                {
                    
                }
            }

        }
    }
}
